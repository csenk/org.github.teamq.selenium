subprojects {
	buildscript {
		repositories {
			maven {
				url project.rootProject.file('lib').toURL()
			}
			mavenLocal()
			mavenCentral()
		}
		
		dependencies {
			classpath group: 'org.github.teamq.gradle', name: 'gwt-plugin', version: '0.0.1-SNAPSHOT'
		}
	}
	
	apply plugin: 'maven'
	
	group = 'org.github.teamq.selenium'
	version = '0.0.1-SNAPSHOT'
	
	repositories {
		maven {
			url 'http://cadas-aimdb-artifactory.comsoft.de:30000/artifactory/anyrepo'
		}
		mavenLocal()
		mavenCentral()
	}
}